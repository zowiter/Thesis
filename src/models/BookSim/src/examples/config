// Topology
topology = mesh;
k = 4;
n = 2;

// Routing
routing_function = dim_order;

// Flow control
num_vcs = 4;
vc_buf_size = 4;

// Traffic
traffic = uniform;
packet_size = 10;

// Simulation
sim_type = latency;
injection_rate = 0.05;
sample_period = 5000;
warmup_periods = 1;
max_samples = 10;
